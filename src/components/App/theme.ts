import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    secondary: {
      light: '#ff4081',
      main: '#f50057',
      dark: '#c51162',
    },
  },
});

export default theme;
